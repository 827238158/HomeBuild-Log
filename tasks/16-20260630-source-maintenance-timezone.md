# 任务：原始数据维护与北京时间修复

## 元信息
- 状态：已完成
- 创建日期：2026-06-30
- 负责人：GPT-5 Codex
- 当前执行Agent：GPT-5 Codex
- 允许修改：`backend/`、`frontend/`、`docs/`、`tasks/`、`README.md`
- 关联任务/决策：阶段 3B 前置维护；任务 15

## 目标
- 提供可审计的来源编辑与安全级联删除，并统一 UTC 存储、带时区 API 和北京时间展示。

## 背景与依据
- 当前来源没有修改、删除入口，正式记录的未知发生时间直接显示为 `unknown`，SQLite 读取系统时间时会丢失时区标记。
- 小主确认采用安全级联删除、修改后标记过期复核，并严格区分录入时间与事情发生时间。

## 范围
- 来源版本、来源引用版本、候选来源版本的数据迁移。
- 来源修改、删除影响预览、安全级联删除和正式记录来源复核 API。
- 前端来源管理入口、删除二次确认、待复核提示及北京时间统一展示。
- UTC 日期时间类型、相关自动化测试和长期文档同步。

## 非目标
- 不实现阶段 3B 的追问、拆分、合并与拒绝。
- 不将来源录入时间伪装为事情发生时间。
- 不删除审计历史，也不自动重写已确认的正式记录内容。

## Need
- `tasks/ACTIVE.md`、任务模板、开发流程与检查清单。
- 来源、候选、正式记录、附件和审计相关模型、迁移、API、前端组件及测试。
- 项目规格、架构、API、领域、UX、决策与路线图文档。

## 依赖与未知项
- 当前项目使用 `D:\Anaconda\envs\homebuild-log\python.exe`，已确认解释器为 Python 3.13.14。
- 工作区存在任务 15 的未提交改动；本任务必须在其现状上增量修改并保留已有内容。

## 实施计划
- [x] 建立 UTC 类型和来源版本迁移。
- [x] 实现来源编辑、影响预览、安全级联删除与来源复核 API。
- [x] 实现前端来源管理、待复核和北京时间展示。
- [x] 补齐后端、前端、迁移和浏览器验收。
- [x] 更新长期文档、Review 与交接记录。

## 验收标准
- [x] 来源可修改并递增版本，旧候选不可确认，已有正式记录显示待复核。
- [x] 删除预览准确，删除后独占记录消失、多来源记录保留且审计仍可查询。
- [x] 所有相关 API 时间带时区，界面按 `Asia/Shanghai` 展示并不再出现英文 `unknown`。
- [x] 后端 Ruff/pytest 与前端 lint/test/build 全部通过。

## 风险与回退
- 删除属于不可恢复操作，必须先展示影响统计并由用户二次确认；数据库删除在单事务中完成。
- 物理附件删除仅处理已无元数据引用的内容寻址文件；失败必须返回清理警告，不能伪装成功。
- 迁移只新增带默认值字段；SQLite 现有系统时间按 UTC 兼容读取，不批量猜测或改写业务发生时间。

## 执行记录
- 2026-06-30：已确认单写者空闲、现有工作区差异和专用 Conda 环境。
- 2026-06-30：实现迁移0009、UTC类型、来源维护/复核API、前端管理入口、北京时间格式化及自动化测试。
- 2026-06-30：真实库迁移前备份到`.local-data/backups/pre-0009-source-maintenance-20260630.sqlite3`，随后成功升级到0009并核对4条来源回填。
- 2026-06-30：浏览器隔离库走通新增、修改、复核、时间线详情和级联删除；控制台无错误，临时验收库已清理。

## 跨模型交接
- 已完成内容：来源版本维护、安全级联删除、来源复核、UTC存储与北京时间展示全部完成。
- 修改文件：`backend/`、`frontend/`、`docs/`、`README.md`与`tasks/`中的本任务相关文件。
- 已执行检查及结果：Ruff通过；后端71项通过；前端lint、34项测试、build、npm audit通过；浏览器验收通过。
- 未完成内容与下一步：无；阶段3B可沿用来源版本与审计边界继续规划。
- 风险、未知项和待确认事项：Windows占用附件时物理删除会返回清理警告，数据库删除保持成功且不伪装文件已清理。
- 工作区是否存在未验收改动：是，任务15的既有未提交改动仍与本任务结果共同保留。
- `tasks/ACTIVE.md`是否已释放：是

## Review
- [x] 已执行 `checklists/implementation-review.md`。
- [x] 已执行 `checklists/docs-update.md` 的更新判断。
- [x] 已在 `tasks/AGENT-ACTIVITY-LOG.md` 追加本任务记录。
- 文档/清单更新：已更新README、项目规格、架构、API契约、决策、领域模型、UX、路线图和任务索引。

## 完成证据与遗留项
- 验收证据：后端71项、前端34项、Ruff、lint、build、npm audit及浏览器完整流程均通过；真实库为迁移0009。
- 遗留项：无。
