# 任务：阶段 3A 文本 AI 录入

## 元信息
- 状态：已完成
- 创建日期：2026-06-29
- 负责人：小主
- 当前执行Agent：GPT-5 Codex（实现）+ deepseek（验收辅助）
- 允许修改：`backend/`、`frontend/`、`docs/`、`tasks/`、`README.md`
- 关联任务/决策：阶段 2B 核心视图；阶段 3 文本 AI 路线图

## 目标
- 交付“AI 提取 → 持久化候选 → 人工编辑/暂存/确认 → 正式记录”的文本录入主链路，并建立可重复执行的样本评测。

## 背景与依据
- `local-rule-v1` 已建立候选确认、原子写入和幂等底座；阶段 3A 在不绕过人工确认的前提下接入 DeepSeek/MiMo。
- 实施范围以小主 2026-06-29 确认的阶段 3 计划为准。

## 范围
- AI 配置、OpenAI 兼容适配器、30 秒主备回退。
- ExtractionRun 与 CandidateBundle 持久化及迁移。
- 提取、候选编辑/暂存/确认、待确认箱和运行记录 API。
- 前端 AI 建议卡片、明确项确认和待确认箱。
- 001/002/003/004/007 黄金样本、评测脚本、测试与文档同步。

## 非目标
- ECharts、追问、拆分、合并、拒绝、OCR、图片识别和 PWA 离线。
- 不修改 `backend/app/local_suggestions.py`，不改变来源不可覆盖边界。

## Need
- `tasks/ACTIVE.md`、任务模板与开发流程。
- 后端配置、应用生命周期、领域模型、建议/确认端点及测试夹具。
- 前端工作区、API、导航、样式及测试结构。
- 样本文档、架构/API/领域/交互/路线图与 Review 清单。

## 依赖与未知项
- 真实 AI 评测依赖小主在本机配置有效 API Key；普通自动化测试必须离线可运行。
- DeepSeek `deepseek-chat` 即将弃用，本任务默认使用 `deepseek-v4-flash`。

## 实施计划
- [x] 实现 AI 配置、适配器、迁移与持久化模型。
- [x] 实现提取、候选、待确认和运行记录 API。
- [x] 实现前端 AI 建议流和待确认箱。
- [x] 建立黄金评测集、评测脚本与自动化测试。
- [x] 同步文档并完成 Review。

## 验收标准
- [x] `auto` 按 DeepSeek → MiMo → 本地规则回退，且总预算不超过 30 秒。
- [x] 候选可持久化、编辑、暂存、幂等确认，原始来源保持不变。
- [x] 待确认箱支持跨来源读取、明确项原子批量确认和移出。
- [x] 离线测试覆盖配置、适配、回退、迁移、API、前端和确认边界。
- [x] 黄金样本和评测脚本可重复执行并报告既定指标。

## 风险与回退
- 云端请求含原始文本；只在显式启用且存在 Key 时发送，密钥不进入日志。
- 模型输出不可信；必须经 JSON/Pydantic/正式记录 Schema 三层校验。
- 迁移可降级删除新表；旧本地建议接口始终保留作为兼容回退。

## 执行记录
- 2026-06-29：核对工作区干净且活动锁空闲，认领任务。
- 实现DeepSeek/MiMo适配、30秒共享预算、本地回退、运行审计、CandidateBundle版本和全部计划API。
- 实现AI建议卡片、明确项确认、候选修改保存、待确认箱和跨来源明确项批量确认。
- 新增001/002/003/004/007黄金JSON与`backend/scripts/evaluate_ai.py`。
- 后端Ruff通过；完整后端测试63项通过；阶段3专项测试10项通过。
- 前端最终TypeScript检查、18项测试和Vite生产构建通过。
- 独立`.browser-data`浏览器环境完成：登录、保存样本004、本地回退、4条明确项确认、问题看板同步。
- 浏览器发现确认状态JSON原地修改未持久化，已增加`flag_modified`并补重新读取回归断言；修复后的再次提权测试因当前执行额度限制未能重跑。
- 未执行真实DeepSeek/MiMo黄金评测：本地未提供API Key，避免未经配置发送原始文本或消耗额度。
- 2026-06-29（deepseek 验收辅助）：在小主本地执行 alembic upgrade head，成功升级到 0007（candidate_bundles）；完整后端测试 63 项通过；阶段 3 专项测试 10 项通过；前端 Vite 生产构建通过。flag_modified 修复已在代码中（extractions.py:702），前述因额度限制未重跑的测试现已通过。验收标准第 4 条（离线测试覆盖）已由自动化验证；第 1/2/3/5 条仍需小主配置 API Key 后浏览器亲验。
- 2026-06-29（deepseek 接口修复）：修复候选确认 422 错误——AI 返回数据缺少必填字段时，后端 detail 为数组导致前端无法解析。修复内容：(1) 校验错误 detail 从数组改为可读中文，(2) 新增 `_fill_missing_required` 自动补全 AI 漏掉的必填字段，(3) 更新系统 prompt 逐类列出必填字段。修复后小主配置 API Key 完成浏览器亲验，AI 建议卡片 → 确认 → 正式记录整链路打通。
- 2026-06-29（小主验收通过）：AI 自动处理文本确认完成，阶段 3A 关闭。

## 跨模型交接
- 已完成内容：阶段3A代码、迁移、API、前端、评测集、文档和离线验收已完成。
- 修改文件：`backend/`、`frontend/`、`docs/`、`README.md`、`.gitignore`和任务记录。
- 已执行检查及结果：Ruff通过；后端63项完整测试和10项专项测试通过；前端18项测试、TypeScript和生产构建通过；浏览器主链路通过并修复一项重载缺陷。
- 未完成内容与下一步：配置真实API Key后分别运行DeepSeek/MiMo黄金评测并由小主完成浏览器亲验；阶段3B/3C另行授权。
- 风险、未知项和待确认事项：修复JSON重载缺陷后的后端重跑因执行额度限制未完成；真实模型质量、成本和网络表现需要验证。
- 工作区是否存在未验收改动：是。
- `tasks/ACTIVE.md`是否已释放：是

## Review
- [x] 已执行 `checklists/implementation-review.md`。
- [x] 已执行 `checklists/docs-update.md` 的更新判断。
- [x] 已在 `tasks/AGENT-ACTIVITY-LOG.md` 追加本任务记录。
- 文档/清单更新：已更新README、产品规格、架构、API、决策、领域模型、交互、路线图和进展摘要。

## 完成证据与遗留项
- 验收证据：后端 63 + 10 专项测试通过；前端 build 通过；flag_modified 修复确认有效；alembic upgrade head 已执行。
- 遗留项：真实供应商评测与小主亲验（需 API Key）；阶段 3B/3C 按路线图另行授权。
