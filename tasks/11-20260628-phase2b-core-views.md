# 任务：阶段 2B 核心视图与基础搜索

## 元信息
- 状态：已完成
- 创建日期：2026-06-28
- 负责人：小主
- 当前执行Agent：GPT-5 Codex
- 允许修改：`backend/`、`frontend/`、`README.md`、`docs/`、`tasks/`
- 关联任务/决策：阶段 2A 领域模型与本地建议卡片、路线图阶段 2

## 目标
- 基于阶段 2A 的共享正式记录实现时间线、账本、问题看板、空间档案、基础搜索和统一详情，并交付给小主亲自验收。

## 背景与依据
- 阶段 2A 已形成来源、八类正式记录、共享实体、关系和原子确认闭环；阶段 2 尚缺少查询投影与核心视图。
- 基线提交为 `2e95932 阶段2-A`，用户允许按本任务需要重构或覆盖 2A 实现。

## 范围
- 后端只读投影服务及时间线、账本、问题看板、空间档案和基础搜索 API。
- 审计目标过滤、来源附件详情和统一记录详情追溯。
- 前端响应式导航、六个工作视图、筛选状态和统一详情面板。
- 自动化检查、长期文档同步和小主亲验清单。

## 非目标
- 首页及采购、决策、调研、待办独立列表。
- AI、OCR、持久化识别任务、PWA离线、Tailscale部署、导入导出和备份恢复。
- 拖拽看板、复杂图表、前端路由库、FTS或模糊搜索。

## Need
- `docs/03-api-contracts.md`、`docs/06-domain-model.md`、`docs/07-ux-visualization.md`。
- 当前领域模型、API、来源/审计接口、前端工作区和测试。
- FastAPI 0.128、SQLAlchemy 2.x、React 19.2.7 的 Context7 当前文档。

## 依赖与未知项
- 不新增业务表或破坏性迁移；投影基于现有模型和关系表。
- 用户亲验前任务只能进入“待验收”，不得标记阶段 2 完成或释放写入锁。

## 实施计划
- [x] 建立后端查询投影底座与详情追溯扩展。
- [x] 实现五个投影/搜索接口和后端边界测试。
- [x] 实现前端导航、核心视图、筛选和详情面板。
- [x] 完成全量检查、Review、文档同步和亲验清单。

## 验收标准
- [x] 四个核心视图与基础搜索读取同一正式记录，状态、金额和关联语义一致。
- [x] 所有结果能回溯来源、附件、关联记录和审计历史。
- [x] 7条真实样本及金额、时间、空间、归档、搜索边界通过自动化验证。
- [x] 后端pytest/Ruff与前端lint/test/build通过。
- [x] 小主亲自验收通过后才能正式收口阶段 2。

## 小主亲验清单
1. 按README分别启动后端与前端，使用自己的本地管理员密码登录。
2. 在时间线确认有日期记录按日期分组，无日期记录进入“时间待补充”，筛选后结果正确。
3. 在账本确认样本001显示采购总额1100元、实际支出500元、待付600元，并能打开计算相关记录详情。
4. 在问题看板确认样本004仍处于实际问题状态；调整状态后，关联决策和待办内容不被修改。
5. 在空间档案选择房屋、房间和局部构件，确认父空间能聚合后代记录且不重复计数。
6. 搜索中文关键词及类型/状态/日期条件，打开结果并核对原始来源、附件、关联记录和审计历史。
7. 归档一条测试记录后确认其从核心视图和搜索默认结果消失，恢复后重新出现。
8. 将浏览器缩至手机宽度，确认导航可横向滚动，页面本身没有横向溢出，金额、筛选、卡片和详情均可操作。

## 风险与回退
- 金额汇总不得把采购总额和实际资金流混为一谈；按币种分组且只用显式关系计算待付。
- 未知日期不得使用创建时间伪装；未知项单独展示。
- `2e95932`是稳定回退点；如重构 2A，必须保持现有测试和契约兼容。

## 执行记录
- 2026-06-28：任务已认领；Context7 已核对 FastAPI 0.128、SQLAlchemy 2.x 和 React 19.2.7 当前文档。
- 2026-06-28：实现服务端批量投影、五个查询接口、审计目标过滤和来源附件详情；无数据库迁移。
- 2026-06-28：实现响应式核心导航、时间线、账本、问题看板、空间档案、搜索和统一详情。
- 2026-06-28：浏览器核验桌面与390px手机宽度，修复详情空间名称缺失与整页横向溢出；控制台无错误。
- 2026-06-28：全量检查通过，任务进入待小主验收状态，活动锁继续保留。
- 2026-06-28：小主确认亲自验收通过；阶段 2 正式收口，活动锁已释放。

## 跨模型交接
- 已完成内容：后端只读投影与五个接口、前端六视图导航和统一详情、测试与长期文档同步。
- 修改文件：`backend/app/projections.py`、视图/来源/审计/领域API及测试；`frontend/src/CoreViews.tsx`、请求层、入口、样式及测试；README、架构/API/决策/交互/路线图/进展和任务文档。
- 已执行检查及结果：Ruff通过；后端53项pytest通过；前端lint、17项测试和build通过；npm audit为0漏洞；`git diff --check`无空白错误；桌面与390px浏览器核验通过。
- 未完成内容与下一步：本任务无未完成项；后续阶段须另建任务授权。
- 风险、未知项和待确认事项：基础搜索不含FTS、模糊排名和重复检测，这些仍属于阶段 5。
- 工作区是否存在未验收改动：否；当前改动已由小主验收，但尚未提交。
- `tasks/ACTIVE.md`是否已释放：是。

## Review
- [x] 已执行 `checklists/implementation-review.md`，并在用户验收后释放活动锁。
- [x] 已执行 `checklists/docs-update.md` 的更新判断。
- [x] 已在 `tasks/AGENT-ACTIVITY-LOG.md` 追加本任务记录。
- 文档/清单更新：已更新README、架构、API契约、决策、交互、路线图、进展和任务文档；产品范围、领域模型、风格与开发流程未变化，无需修改。

## 完成证据与遗留项
- 验收证据：53项后端测试、Ruff、17项前端测试、lint、build、npm audit、diff检查与浏览器响应式核验均通过。
- 遗留项：无；阶段 3、4、5 均保持未启动。
