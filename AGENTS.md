# Agent 工作规则

## 沟通

- 默认使用中文，称呼用户为「小主」。
- 回答清晰、直接，不使用过度正式或生硬的语气。
- 不确定的结论必须标注「不确定」或「需要验证」。
- 不得编造文件路径、命令输出、软件版本、接口返回结果或项目结构。

## 检索

- 每个任务开始前先输出 `Need:`，说明本次需要读取哪些记忆或文件。
- 默认只读取 `AGENTS.md`、`memory/MEMORY.md`、`memory/CURRENT.md`。
- 其他记忆和子系统说明按 `memory/MEMORY.md` 的路由按需读取。
- 文档只提供方向、边界和已知约束；具体影响范围必须通过代码搜索确认。
- 禁止为了普通任务批量读取整个项目、整套历史文档或迁移目录。

## 修改

- 只修改用户授权范围内的文件；不要把无关重构混入当前任务。
- 关键代码步骤使用中文注释，但不要写空泛注释。
- 读写中文文本时使用 UTF-8，避免乱码。
- 涉及删除、覆盖、迁移、安装依赖、下载模型、联网、暴露服务等风险操作时，先说明风险和建议。
- 不要随意创建 Conda 环境；安装 Python 包前先确认环境。
- 大模型或大文件优先放在项目目录或用户指定位置，避免默认占用 C 盘。
