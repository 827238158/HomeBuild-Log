# 操作日志

只记录会影响未来判断的重要操作、迁移、部署、失败恢复和长期有效变更。不要写 Agent 身份、锁、阅读流水或普通进度。

| 日期 | 事项 | 摘要 |
| --- | --- | --- |
| 2026-07-20 | 文档系统替换为 AI 外部记忆层 | 旧项目管理式文档系统改为极简活文档；默认入口后续收敛为 `AGENTS.md`、`memory/MEMORY.md`、`memory/CURRENT.md`。 |
| 2026-07-21 | 记忆文件收纳到专门目录 | 将运行记忆文件移动到 `memory/`，根目录保留 `AGENTS.md`、`README.md`、`DESIGN.md`。 |
| 2026-08-03 | Ubuntu Docker 验证环境初始化 | `ubuntu26`（Ubuntu 26.04）已安装 Docker Engine 29.7.1、Buildx 0.36.0、Compose 5.3.1；Docker 服务开机自启，`hello-world` 容器验证通过。Docker Hub 拉取需临时启动 Mihomo，验证后代理已关闭；HomeBuild Log 项目镜像与持久化部署尚未验收。 |
| 2026-08-03 | HomeBuild Log 空数据容器实机验证 | 当前工作树已传至 `/home/pawel/workspace/HomeBuild-Log`，镜像 `homebuild-log:4a-20260715` 构建成功；前端首页、健康接口、SQLite/存储、只读根文件系统和容器重启持久化均通过。修复 Linux 运行锁缺少 PyJWT 与部署目录数值 UID/GID 赋权失败；真实数据、整机重启和局域网访问未验收。 |
| 2026-08-04 | 活文档与 Agent 记忆漂移检修 | 继续使用 `AGENTS.md` 与 `memory/` 轻量记忆层，未恢复旧 `docs/`、`tasks/`、`checklists/` 文档树；对齐 README 能力边界和 Ubuntu 状态，修正 Ubuntu 版本、迁移 current head、DESIGN 候选关联与 LOG 检索路由。 |
| 2026-08-04 | 项目记忆检索与归档提醒闭环 | 项目级 Hook 形成“定向检索—高信号失败摘要—结束归档提醒”链路：不自动写记忆、不保存完整命令或日志，普通成功和单次临时失败不升级；详细逻辑收纳在 `.codex/hooks/`，不重复写入 `AGENTS.md`。 |
| 2026-08-09 | README 改为用户手动维护 | `README.md` 不再随代码、部署或记忆变化由 Agent 自动更新；只有用户在当前任务中明确授权时才能修改，长期约束已写入 `AGENTS.md`。 |
| 2026-08-09 | Ubuntu 由 GitHub 源码更新容器 | `ubuntu26` 的 `/home/pawel/workspace/HomeBuild-Log` 已初始化为跟踪 `origin/main` 的 Git 仓库，并从提交 `425867a885ff` 构建镜像 `homebuild-log:425867a885ff`。Compose 容器启动后为 `healthy`，健康接口的应用、SQLite 与存储检查均为 `ok`，首页返回 HTTP 200；构建期间临时使用 Mihomo，结束后服务和 7890/9090 监听均已关闭。服务器当前仍为空数据验证环境，未做真实数据迁移验收。 |
| 2026-08-09 | Windows 真实数据迁移到 Ubuntu | Windows `.local-data` 经 SQLite 一致性快照和双层 SHA-256 校验后导入 `ubuntu26`，源数据和本地迁移包均保留。容器将数据库从 `0015_merge_procurement` 升级到 `0018_unify_relations`；迁移保护检查和计数核对确认 60 条记录、15 条问题、4 条关系、1 条旧待办备份无缺失。Compose 容器为 `healthy`，应用、SQLite 和存储检查均为 `ok`，Windows `127.0.0.1:8000` SSH 映射已恢复。 |
| 2026-08-18 | 独立踩坑记录功能落地 | 新增 `0019_add_pitfall_logs`，用独立踩坑与处理记录表保存轻量手工日志；处理状态由处理记录数量推导，AI 只在手动触发时汇总且不回写原始数据。 |
| 2026-08-20 | Windows 真实数据库升级到 0019 | 使用 SQLite 在线备份生成并校验唯一恢复点后，将真实数据库从 `0015_merge_procurement` 升级到 `0019_add_pitfall_logs`；完整性、外键和关键计数通过，健康接口 current/expected 一致。旧的 4 份 `0012–0015` 前备份已按授权删除。Windows 启动增加迁移门禁，健康接口增加 revision 检查，前端保留后端错误码。 |
| 2026-08-21 | 踩坑记录功能验收并同步远端 | 用户完成实机验收；功能代码、迁移、测试、启动保护和项目记忆以提交 `338866efaef9b526190ebdf3bdffb41673102e44` 推送到 `origin/main`。本地设计 Brief 已加入忽略规则，数据库、备份、密钥、依赖与构建产物未进入提交。 |
| 2026-08-21 | Ubuntu 真实数据容器升级到 0019 | `ubuntu26` 已快进到提交 `3cc054144ec1`，使用 Docker 国内镜像、npmmirror 和清华 PyPI 构建镜像 `homebuild-log:3cc054144ec1`。停止写入后完整备份 Ubuntu 唯一真实数据，SHA-256 校验通过；数据库从 `0018_unify_relations` 升级到 `0019_add_pitfall_logs`，64 条记录、57 条来源、16 条问题、5 条关系和 1 条旧待办备份均无缺失，新踩坑表初始为空。容器为 `healthy`，应用、revision、SQLite 和存储检查均为 `ok`，首页 HTTP 200；Mihomo 已关闭，7890/9090 未监听，旧镜像与迁移前备份保留。 |
| 2026-08-21 | Ubuntu Docker 开放家庭局域网访问 | `ubuntu26` 的 Compose 映射由 `127.0.0.1:8000` 改为仅绑定有线地址 `192.168.1.17:8000`，Windows 同网段直连首页和健康接口通过，容器保持 `healthy`、`restart: unless-stopped`，数据库 revision、SQLite 与存储均为 `ok`；UFW 当前不活动，已保存仅允许 `192.168.1.0/24` 访问该端口的规则但未贸然启用，Mihomo 7890/9090 保持关闭。手机实机登录和整机重启仍待验收。 |
| 2026-08-23 | Ubuntu 真实数据容器更新到移动端修复版 | `ubuntu26` 已快进到提交 `f255e5a89414`，用一次性临时 Dockerfile 通过 DaoCloud、npmmirror 和清华 PyPI 构建镜像 `homebuild-log:f255e5a89414`，全程未启用 Mihomo。停写后备份 `.local-data` 并通过 SHA-256 校验；新容器为 `healthy`，数据库仍为 `0019_add_pitfall_logs`，SQLite 完整性、存储和启动日志正常，65 条记录、59 条来源、16 条问题、7 条关系、1 条旧待办备份和 1 条踩坑均保留。服务继续只绑定 `192.168.1.17:8000`，Ubuntu 与同网段 Windows 首页、健康接口和登录接口可达；UFW 仍不活动，手机真实登录尚未验收。 |
| 2026-08-26 | 手机日期时间控件修复发布并部署 | 手机原生日期/时间控件收缩修复与前序部署记忆以提交 `1895a5f9c11b`（`修复手机界面BUG`）推送至 `origin/main`。`ubuntu26` 使用 DaoCloud、npmmirror 和清华 PyPI 构建并切换到同标签镜像，全程未启用 Mihomo；停写备份 SHA-256 通过。容器为 `healthy`，数据库仍为 `0019_add_pitfall_logs`，SQLite 完整性正常，65 条记录、59 条来源、16 条问题、7 条关系、1 条旧待办备份和 1 条踩坑均保留；服务继续绑定 `192.168.1.17:8000`，Ubuntu 与同网段 Windows 访问正常。 |
| 2026-09-08 | Ubuntu Docker 更新到手机和平板修复版 | `ubuntu26` 已快进到提交 `bc1115e92e8e`，使用一次性临时 Dockerfile 和 DaoCloud、npmmirror、清华 PyPI 构建并部署镜像 `homebuild-log:bc1115e92e8e`。因直连 GitHub 不可用，仅在拉取代码期间临时启用 Mihomo，构建和部署结束后服务及 7890/9090 监听均已关闭。停写备份 `.local-data` 的 SHA-256 校验通过；新容器为 `healthy`，数据库仍为 `0019_add_pitfall_logs`，SQLite 完整性正常，81 条记录、69 条来源、19 条问题、13 条关系、1 条旧待办备份、1 条踩坑和 0 条处理记录均保留。服务继续绑定 `192.168.1.17:8000`，Ubuntu 与同网段 Windows 首页和健康接口均验证通过。 |
| 2026-09-09 | 图表筛选交互口径明确 | 问题看板状态与严重程度采用交集筛选，两张图和明细统一使用服务端筛选结果；时间线趋势按月点击转换为整月日期范围。表单草稿与已应用条件分离，取消图表条件保留其他已应用条件。本地实现、回归和 GitHub 推送完成，Ubuntu 部署见下条。 |
| 2026-09-09 | 图表筛选版部署到 Ubuntu | Ubuntu 直连 GitHub 超时且临时代理发生 GnuTLS 握手中止；确认本地 `main` 与 GitHub `origin/main` 同为 `1d3d676584a6` 后，通过校验过的完整 Git bundle 将服务器仓库 fast-forward 到同一提交。使用 DaoCloud、npmmirror 和清华 PyPI 构建并部署 `homebuild-log:1d3d676584a6`，结束后 Mihomo 和代理端口均关闭。停写备份 SHA-256 通过；容器为 `healthy`，数据库保持 `0019_add_pitfall_logs`，SQLite 完整性正常，88 条记录、73 条来源、23 条问题、17 条关系、1 条旧待办备份、1 条踩坑和 0 条处理记录均保留；Ubuntu 与同网段 Windows 访问均通过。 |
| 2026-09-09 | 删除本次部署恢复点 | 用户确认无需保留后，已删除 `local-data-20260909T011504Z.tar.gz`、对应 SHA-256 文件和 `.last-upgrade`；此前中断步骤产生的重复备份也已删除。运行中的 `.local-data` 未改动，容器继续使用 `homebuild-log:1d3d676584a6` 且健康接口全部为 `ok`；本次部署不再具备一键数据回退条件。 |
| 2026-09-13 | 空间界面修复版部署到 Ubuntu | `ubuntu26` 通过 GitHub 直连 fast-forward 到提交 `e9bea3a8479a77ed035dc69c9115534fbff6d97a`，服务器工作树干净，使用 DaoCloud、npmmirror 和清华 PyPI 构建并部署镜像 `homebuild-log:e9bea3a8479a`，全程未启用 Mihomo。两个提交间没有数据库迁移文件变化，按用户此前决定未创建恢复点；容器为 `healthy`，数据库保持 `0019_add_pitfall_logs`，SQLite 完整性正常，89 条记录、73 条来源、23 条问题、19 条关系、1 条旧待办备份、2 条踩坑和 1 条处理记录均保留；Ubuntu 与同网段 Windows 访问均通过。一次性临时 Dockerfile 未改仓库构建文件，前端构建仅有既有 EChart 大分块提示；结束后 Mihomo 为 `inactive`，7890/9090 未监听。 |
| 2026-09-16 | 简化项目记忆职责与 Hook 审计 | MEMORY 只维护稳定事实和路由，CURRENT 只保留带日期的最近运行状态、待验收及待核实问题，完成事项归入 LOG；合并 PITFALLS 重复前端章节。Hook 采用非阻断审计提醒，不自动写记忆，不以文件是否更新作为完成标准；模拟信号和核实后的误报不为凑数归档。 |
| 2026-09-21 | 最新修复版部署到 Ubuntu | `ubuntu26` 通过 GitHub 直连 fast-forward 到提交 `cd76370c0f50`。清华 PyPI 未同步锁定的 `alembic==1.18.5`，核对国内镜像后改用阿里云 PyPI，与 DaoCloud、npmmirror 一起完成镜像构建；镜像 `homebuild-log:cd76370c0f50` 的 OCI 版本标签正确，全程未启用 Mihomo。两个提交间没有数据库迁移文件变化，本次未创建恢复点；容器为 `healthy`，数据库保持 `0019_add_pitfall_logs`，SQLite 完整性正常，95 条记录、79 条来源、23 条问题、21 条关系、1 条旧待办备份、2 条踩坑和 1 条处理记录均保留。Ubuntu 与同网段 Windows 访问通过，未认证未知 API 返回 401 且不是 HTML；合法 token 场景仍按 CURRENT 待核实项处理。 |
| 2026-09-24 | AI 模型配置更新 | 默认配置、本机私有配置和 `ubuntu26` 运行中容器的持久化配置均改为小米 `mimo-v2.6-pro` 优先、DeepSeek `deepseek-flash` 备用；密钥与其他配置未变。容器从有效配置读取到新模型且保持 `healthy`；未向模型服务发起真实请求，供应商接口兼容性待实际使用验证。 |
| 2026-09-24 | 恢复每轮记忆核对 | 项目规则要求结束前核对运行状态、当前未完成工作、长期决策、可复用陷阱和已验证命令；需要后续接手的本地工作写入 CURRENT。Stop Hook 每轮给非阻断提示，失败线索单独提示，不根据文件改动强制归档，也不自动写记忆。 |
| 2026-09-24 | 记忆核对改为 Stop Hook 一次性续行 | 仅在 AGENTS 写规则会受长上下文影响，非阻断 `systemMessage` 也不足以保证结束前核对。首次 Stop 以 `decision: block` 注入核对任务，续行后按 `stop_hook_active` 放行；是否写入仍按事实价值判断，不自动改文档。 |
| 2026-09-24 | 记录页 UI 调整部署到 Ubuntu | GitHub 直连超时、临时 Mihomo 代理又发生 GnuTLS 握手中止；确认本地 `main` 与 GitHub `origin/main` 一致后，经完整 Git bundle 校验并 fast-forward 到 `1a30fa93bf4c`。使用 DaoCloud、npmmirror 和阿里云 PyPI 构建并部署镜像 `homebuild-log:1a30fa93bf4c`，OCI 版本标签正确；两个提交间没有数据库迁移文件变化，按用户此前决定未创建恢复点。容器为 `healthy`，数据库保持 `0019_add_pitfall_logs`，SQLite 完整性正常，97 条记录、80 条来源、23 条问题、22 条关系、1 条旧待办备份、2 条踩坑和 2 条处理记录均保留；Ubuntu 与同网段 Windows 访问通过，结束后 Mihomo 和代理端口均关闭。 |
| 2026-09-25 | 录入页布局与模型选择部署到 Ubuntu | 前端改为宽屏快速记录、同行 44px 附件和保存按钮、四选项模型下拉和悬浮来源操作；提取接口支持 `mimo`、`deepseek`、`auto`、`local`，保留 `ai` 兼容。提交 `0d12038aab66` 经用户授权推送 GitHub `main`，Ubuntu 直连快进，使用 DaoCloud、npmmirror、阿里云 PyPI 构建并切换同标签镜像。无迁移变化；停写后备份 `.local-data` 并校验 SHA-256 与归档可读性，旧镜像及 `.last-upgrade` 保留。容器 `healthy`、revision `0019_add_pitfall_logs`、SQLite 完整性正常，97 条记录、80 条来源、23 条问题、22 条关系、1 条旧待办备份、2 条踩坑和 2 条处理记录均保留；Ubuntu 与同网段 Windows 首页 HTTP 200，服务仍仅绑定 `192.168.1.17:8000`，Mihomo 和代理端口关闭。自动化前端 125 项与相关后端 17 项通过，真实设备和真实模型请求尚未验收。 |
| 2026-09-25 | 语音录入与内网 HTTPS 方案落地到源码 | 录入页新增页面内录音并生成 WAV，停止后经鉴权接口调用现有 `mimo-v2.6-pro` 并把结果追加到草稿；HTTPS 采用可选 Caddy profile 与内网 CA，先并行 443，再在设备信任证书和验收后收回局域网 HTTP。真实小米接口对本机合成中文语音成功转写，未改数据库；Ubuntu 尚未部署，Caddy 镜像待用户按项目规则手动准备。 |
| 2026-09-25 | 语音录入与内网 HTTPS 并行部署 | 用户授权代为准备镜像并确认长期采用 Windows 修改、GitHub 同步、Ubuntu 拉取及重建容器。Ubuntu 直连快进到 `6e3b003`；Caddy 经 DaoCloud 官方镜像代理取得，摘要 `sha256:6aeddd44c3078b0f9a35206472a11420648a79c184603ef95957d0a20044cb2b`；应用用 DaoCloud、npmmirror、阿里云 PyPI 构建 `homebuild-log:825e98ee2a29`。停写后真实数据归档经 SHA-256 与可读性校验，CA 数据卷另存权限 600 的归档。新应用健康，Caddy 与应用均为 `unless-stopped`；HTTP 8000 和 HTTPS 443 并行，Ubuntu 与 Windows 通过 CA 验证 HTTPS 健康接口 200，未认证转写请求 401。正式容器鉴权转写接口对 4.5 秒合成中文 WAV 返回忠实转写，接口全程约 1.7 秒、模型处理约 1.6 秒。数据库 `0019_add_pitfall_logs`、完整性 `ok`，97 条记录、80 条来源、23 条问题、22 条关系、2 条踩坑和 2 条处理记录保留。Windows 当前用户已信任 CA；实际浏览器、手机和平板尚未验收，故未关闭局域网 HTTP。 |
